{
  "name": "city-3",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix UI-stuff",
    "build": "npm run build --prefix UI-stuff",
    "start": "npm run start --prefix UI-stuff",
    "lint": "npm run lint --prefix UI-stuff"
  }
}
